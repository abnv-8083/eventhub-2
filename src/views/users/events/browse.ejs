<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../../partials/head') %>
</head>
<body class="bg-mesh">
    <%- include('../../partials/navbars/user-nav') %>

    <div class="browse-page">

        <!-- Hero Search -->
        <div class="browse-hero animate stagger-1">
            <p class="text-dim small text-uppercase fw-bold mb-2" style="letter-spacing:0.1em; color:var(--primary);">
                <i class="fi fi-rr-sparkles me-1"></i> Discover Events
            </p>
            <h2 class="fw-bold text-white mb-1">Find Your Next Experience</h2>
            <p class="text-dim mb-4" style="font-size:0.95rem;"><%= total %> events available right now</p>
            <div class="hero-search-wrap">
                <i class="fi fi-rr-search"></i>
                <input type="text" id="evtSearch" class="input w-100" placeholder="Search events by name..." value="<%= filters.search %>">
            </div>
        </div>

        <!-- Filter Toolbar -->
        <div class="filter-toolbar animate stagger-2">
            <span class="text-dim" style="font-size:0.8rem; white-space:nowrap;">Filter by:</span>
            <select id="evtCategory" class="filter-select">
                <option value="all" <%= filters.category === 'all' ? 'selected' : '' %>>All Categories</option>
                <% categories.forEach(cat => { %>
                    <option value="<%= cat._id %>" <%= filters.category === cat._id.toString() ? 'selected' : '' %>><%= cat.name %></option>
                <% }) %>
            </select>
            <select id="evtSort" class="filter-select">
                <option value="newest" <%= filters.sort === 'newest' ? 'selected' : '' %>>Newest First</option>
                <option value="oldest" <%= filters.sort === 'oldest' ? 'selected' : '' %>>Oldest First</option>
                <option value="date-asc" <%= filters.sort === 'date-asc' ? 'selected' : '' %>>Upcoming First</option>
            </select>
            <button id="applyBtn" class="btn btn-primary btn-sm px-4" style="height:38px; border-radius:8px; white-space:nowrap;">Apply</button>
            <button id="clearBtn" class="btn btn-outline btn-sm px-3" style="height:38px; border-radius:8px; white-space:nowrap;">
                <i class="fi fi-rr-refresh me-1"></i> Clear
            </button>
            <span class="ms-auto text-dim" style="font-size:0.82rem; white-space:nowrap;"><%= total %> result<%= total !== 1 ? 's' : '' %></span>
        </div>

        <!-- Events Grid -->
        <% if(events && events.length > 0) { %>
        <div class="event-grid animate stagger-3">
            <% events.forEach(event => { %>
            <div class="event-card" onclick="window.location.href='/user/events/<%= event._id %>'">
                <div style="position:relative;">
                    <% if(event.isFeatured) { %><span class="featured-badge">&#11088; Featured</span><% } %>
                    <% if(!event.hasAvailability) { %><span class="soldout-badge">Sold Out</span><% } %>

                    <!-- Wishlist Heart Button -->
                    <button
                        class="wish-btn <%= event.isWishlisted ? 'wishlisted' : '' %>"
                        id="wish-<%= event._id %>"
                        onclick="toggleWishlist(event, '<%= event._id %>', this)"
                        title="<%= event.isWishlisted ? 'Remove from wishlist' : 'Add to wishlist' %>"
                    >
                        <i class="fi fi-<%= event.isWishlisted ? 'sr' : 'rr' %>-heart"></i>
                    </button>

                    <% if(event.banners && event.banners.length > 0) { %>
                        <img src="<%= event.banners[0] %>" class="event-card-img" alt="<%= event.title %>">
                    <% } else { %>
                        <div class="event-card-img-ph"><i class="fi fi-rr-picture" style="font-size:2.5rem;color:rgba(255,255,255,0.12);"></i></div>
                    <% } %>
                </div>
                <div class="event-card-body">
                    <div class="d-flex justify-content-between align-items-center mb-2">
                        <span class="event-cat-tag"><%= event.category?.name || 'Event' %></span>
                        <% if(event.hasAvailability) { %><span style="width:7px;height:7px;border-radius:50%;background:#2ecc71;display:inline-block;" title="Available"></span><% } %>
                    </div>
                    <h6 class="fw-bold text-white mb-2" style="font-size:0.96rem;line-height:1.35;flex:1;"><%= event.title %></h6>
                    <div class="event-meta-row mb-1">
                        <i class="fi fi-rr-calendar"></i>
                        <span><%= new Date(event.startDate).toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'}) %></span>
                        <span style="opacity:0.4;">&middot;</span>
                        <span><%= event.startTime %></span>
                    </div>
                    <div class="event-meta-row mb-3" style="overflow:hidden;">
                        <i class="fi fi-rr-marker" style="flex-shrink:0;"></i>
                        <span style="white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"><%= event.location.address %></span>
                    </div>
                    <div class="d-flex justify-content-between align-items-center mt-auto pt-2" style="border-top:1px solid rgba(255,255,255,0.06);">
                        <span class="event-price-badge">
                            <% if(event.minPrice === 0) { %><span style="color:#2ecc71;">Free</span><% } else { %>From &#8377;<%= event.minPrice.toLocaleString('en-IN') %><% } %>
                        </span>
                        <span style="color:rgba(255,255,255,0.3);font-size:0.78rem;">View &rarr;</span>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>

        <!-- Pagination -->
        <% if(totalPages > 1) { %>
        <div class="d-flex justify-content-between align-items-center mt-5 pt-2">
            <p class="text-dim mb-0 small">Page <strong class="text-white"><%= currentPage %></strong> of <%= totalPages %></p>
            <div class="d-flex gap-2">
                <% if(currentPage > 1) { %><button class="action-btn" onclick="changePage(<%= currentPage-1 %>)"><i class="fi fi-rr-angle-left"></i></button><% } %>
                <% for(let i=1;i<=totalPages;i++) { %><button class="action-btn <%= i==currentPage?'active-page':'' %>" onclick="changePage(<%= i %>)"><%= i %></button><% } %>
                <% if(currentPage < totalPages) { %><button class="action-btn" onclick="changePage(<%= currentPage+1 %>)"><i class="fi fi-rr-angle-right"></i></button><% } %>
            </div>
        </div>
        <% } %>

        <% } else { %>
        <div class="empty-state animate stagger-3">
            <div class="empty-icon"><i class="fi fi-rr-search"></i></div>
            <h5 class="text-white mb-2">No Events Found</h5>
            <p class="text-dim mb-4">Try adjusting your search or clearing the filters.</p>
            <button onclick="window.location.href=window.location.pathname" class="btn btn-outline px-4">Clear Filters</button>
        </div>
        <% } %>

    </div><!-- /browse-page -->

    <script>
        const IS_LOGGED_IN = <%= isLoggedIn %>;

        // â”€â”€â”€ Wishlist Toggle â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
        async function toggleWishlist(e, eventId, btn) {
            e.stopPropagation();
            if (!IS_LOGGED_IN) {
                window.location.href = '/user/login?message=Please login to save events';
                return;
            }
            try {
                const res = await axios.post(`/user/events/${eventId}/wishlist`, {}, {
                    headers: { 'Accept': 'application/json' }
                });
                if (res.data.success) {
                    const active = res.data.wishlisted;
                    btn.classList.toggle('wishlisted', active);
                    btn.querySelector('i').className = active ? 'fi fi-sr-heart' : 'fi fi-rr-heart';
                    btn.title = active ? 'Remove from wishlist' : 'Add to wishlist';
                    Toast[active ? 'success' : 'info'](res.data.message);
                }
            } catch (err) {
                if (err.response?.status === 401) {
                    window.location.href = '/user/login?message=Please login to save events';
                } else {
                    Toast.error('Could not update wishlist. Try again.');
                }
            }
        }

        // â”€â”€â”€ Filters & Pagination â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
        function changePage(p) {
            const url = new URL(window.location.href);
            url.searchParams.set('page', p);
            window.location.href = url.toString();
        }
        function applyFilters() {
            const search = document.getElementById('evtSearch').value.trim();
            const category = document.getElementById('evtCategory').value;
            const sort = document.getElementById('evtSort').value;
            const url = new URL(window.location.origin + window.location.pathname);
            if (search) url.searchParams.set('search', search);
            if (category !== 'all') url.searchParams.set('category', category);
            if (sort !== 'newest') url.searchParams.set('sort', sort);
            url.searchParams.set('page', 1);
            window.location.href = url.toString();
        }
        document.addEventListener('DOMContentLoaded', () => {
            document.getElementById('applyBtn').addEventListener('click', applyFilters);
            document.getElementById('clearBtn').addEventListener('click', () => window.location.href = window.location.pathname);
            document.getElementById('evtSearch').addEventListener('keypress', e => { if(e.key==='Enter') applyFilters(); });
        });
    </script>
</body>
</html>
